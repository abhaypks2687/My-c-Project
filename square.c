// WAP to caluclate square of a given number. Number is intered by user.
#include<stdio.h>
int main()
/*{
    int num;
    printf("Enter a number:");
    scanf("%d",&num);
    printf("Square of a number %d is:%d",num,num*num);
    system("pause");Windows पर output दिखाने के लिए
    return 0;
}*/
    {
     int num,x;
    printf("Enter a number:");
    scanf("%d",&x);
    num=x*x;
    printf("Square of a number %d is:%d",x,num);
    return 0;
    }